[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    slow: 标记慢速测试（如实际生成绘本）
    login: 登录相关测试
    tools: 工具页相关测试
    storybook: 绘本生成相关测试

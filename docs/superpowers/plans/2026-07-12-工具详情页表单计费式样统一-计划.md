# 工具详情页表单与计费式样统一 Implementation Plan

> 对应 spec：`docs/superpowers/specs/2026-07-12-工具详情页表单计费式样统一-设计.md`

**Goal:** 将通用工具详情页（`[id]` 路由）的 `ToolCreationForm.DynamicToolPage` 表单与计费布局改造为与 `storybook-generator` 标杆一致的三栏式样，并补齐 section header 副标题与右栏「预计耗时」卡片。

**改动范围:** 单文件 `apps/frontend-user/src/components/tool-detail/ToolCreationForm.tsx`，仅 JSX 布局调整，不动状态、提交、计费等任何逻辑。

---

## 改动文件总览

```
apps/frontend-user/src/components/tool-detail/ToolCreationForm.tsx   # Modify: DynamicToolPage 布局重写
```

- `ToolCreationFormWithTabs` 的表单分支因调用 `DynamicToolPage`，自动随之对齐，无需单独改动。
- `DialogMode` 分支、「开发中」降级分支不动。

---

## Task 1: 改造 DynamicToolPage 布局对齐标杆

**Files:**
- Modify: `apps/frontend-user/src/components/tool-detail/ToolCreationForm.tsx`

- [ ] **Step 1: 重写 `DynamicToolPage` 的 return JSX（约 69-101 行）**

将原单栏 + `rightSlot` 布局：

```tsx
  return (
    <section id="start-creation" className="py-20 bg-[#F8FAFC]">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="text-center mb-12">
          <h2 className="text-3xl md:text-4xl font-bold text-brand-dark mb-4">开始创作</h2>
        </div>
        <div className="max-w-2xl mx-auto">
          <DynamicToolForm
            paramSchema={tool.param_schema!}
            toolId={tool.id}
            onSubmit={handleSubmit}
            onValuesChange={(vals) => setFormValues(vals)}
            disabled={generation.isCreating}
            submitLabel={generation.isCreating ? '正在创建任务...' : '开始生成'}
            rightSlot={
              <PriceEstimatePanel
                estimate={estimate}
                className="mt-6"
              />
            }
          />
        </div>
      </div>

      <ProgressModal
        isOpen={generation.showProgressModal}
        taskId={generation.progressTaskId}
        toolName={tool.name}
        onClose={generation.closeProgressModal}
        onComplete={generation.handleProgressComplete}
      />
    </section>
  );
```

改为三栏布局（类名与 `StorybookForm.tsx:65-106` 完全一致）：

```tsx
  return (
    <section id="start-creation" className="py-20 bg-[#F8FAFC]">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="text-center mb-12">
          <h2 className="text-3xl md:text-4xl font-bold text-brand-dark mb-4">开始创作</h2>
          <p className="text-xl text-gray-500 max-w-2xl mx-auto">
            选择适合您的创作方式，简单几步即可生成专属作品
          </p>
        </div>
        <div className="grid lg:grid-cols-3 gap-8">
          <div className="lg:col-span-2">
            <div className="bg-white rounded-2xl p-8 border border-gray-200 shadow-sm">
              <DynamicToolForm
                paramSchema={tool.param_schema!}
                toolId={tool.id}
                onSubmit={handleSubmit}
                onValuesChange={(vals) => setFormValues(vals)}
                disabled={generation.isCreating}
                submitLabel={generation.isCreating ? '正在创建任务...' : '开始生成'}
              />
            </div>
          </div>
          <div className="lg:col-span-1">
            <div className="sticky top-24">
              <PriceEstimatePanel estimate={estimate} className="mb-6" />
              <div className="bg-white rounded-2xl p-6 border border-gray-200">
                <p className="text-sm text-gray-500 text-center">预计耗时：2-5 分钟</p>
              </div>
            </div>
          </div>
        </div>
      </div>

      <ProgressModal
        isOpen={generation.showProgressModal}
        taskId={generation.progressTaskId}
        toolName={tool.name}
        onClose={generation.closeProgressModal}
        onComplete={generation.handleProgressComplete}
      />
    </section>
  );
```

关键变化：
1. `max-w-2xl mx-auto` 单栏 → `grid lg:grid-cols-3 gap-8` 三栏。
2. 表单外层包 `bg-white rounded-2xl p-8 border border-gray-200 shadow-sm` 白色卡片。
3. 移除 `DynamicToolForm` 的 `rightSlot` 传参；`PriceEstimatePanel` 移到右栏 `sticky top-24`。
4. 新增 section header 副标题（通用文案）。
5. 新增右栏「预计耗时：2-5 分钟」卡片。
6. `ProgressModal` 保持不变。

- [ ] **Step 2: lint 验证**

```bash
cd apps/frontend-user && pnpm lint
```

Expected: 无错误。

---

## Task 2: 人工对比验证

- [ ] **Step 1: 启动开发服务并对比**

```bash
cd apps/frontend-user && pnpm dev
```

人工检查：
1. `/tools/creative-video-generator`：三栏布局（左表单白色卡片 + 右 sticky 价格面板 + 预计耗时卡片 + 副标题）。
2. `/tools/storybook-generator`：两者「开始创作」区视觉一致。
3. `form+dialog` 工具切换表单模式确认三栏（如有）。
4. `dialog-only` 工具不受影响（如有）。

---

## Plan 自检

### Spec 覆盖检查
- ✅ DynamicToolPage 三栏布局 → Task 1
- ✅ section header 副标题 → Task 1
- ✅ 右栏预计耗时卡片 → Task 1
- ✅ 不再传 rightSlot → Task 1
- ✅ WithTabs 表单分支自动对齐（调用 DynamicToolPage）→ 无需单独改动
- ✅ lint 验证 → Task 1 Step 2
- ✅ 人工对比 → Task 2

### Placeholder 检查
- ✅ 无 TBD/TODO
- ✅ 代码块完整
- ✅ 文件路径准确

### 一致性检查
- ✅ 与 `StorybookForm` 布局类名一致（`grid lg:grid-cols-3` / `col-span-2` / `col-span-1` / `sticky top-24` / 白色卡片）
- ✅ 副标题通用文案与 spec 一致
- ✅ 预计耗时文案与标杆一致
- ✅ 不触碰 `DynamicToolForm` / `PriceEstimatePanel` / 三个专属页

### 非范围确认
- ✅ 不改三个专属页
- ✅ 不改共享组件（`rightSlot` 机制保留）
- ✅ 不改后端 / API / seed
- ✅ 不新增共享布局抽象
- ✅ 不处理 `ToolCreationFormWithTabs` 嵌套 section 既有问题（spec 标注非范围）

---

## 备注

- 提交（git commit）时机由用户指示；本计划不强制每步提交。
- 实现遵循 `.ai/coding-standards.md` 与 `.ai/frontend-user-standards.md`。

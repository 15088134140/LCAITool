# 阶段一核心用户系统 - 测试实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成阶段一核心用户系统的全量分层测试，发现并闭环所有问题，输出验收级测试报告，确保所有功能100%可用，Mock接口全部清理

**Architecture:** 采用四层测试架构：1) 单元测试层验证Service核心逻辑 2) 接口测试层验证API完整性 3) Playwright E2E层模拟真实用户操作 4) 手工验证层做交叉验证和Mock清理

**Tech Stack:** pytest, pytest-asyncio, Playwright, PostgreSQL, HTTPX, coverage.py

---

## 前置检查与环境准备

### Task 0: 测试环境配置与服务启动

**Files:**
- Modify: `apps/backend/tests/conftest.py:12`
- Modify: `apps/backend/.env` (if exists)
- Check: `apps/backend/app/core/database.py`

- [ ] **Step 1: 配置测试数据库连接为PostgreSQL**

修改 `apps/backend/tests/conftest.py`，替换SQLite配置为PostgreSQL：

```python
# 注释掉原SQLite配置
# TEST_DATABASE_URL = "sqlite+aiosqlite:///./test.db"

# 使用PostgreSQL测试数据库
TEST_DATABASE_URL = "postgresql+asyncpg://shuqitec:Shuqi%242026@pgm-7xv29038ivah5apreo.pg.cn-guangzhou.rds.aliyuncs.com:5432/lcaitool"
```

- [ ] **Step 2: 确保后端服务可正常启动**

```bash
cd apps/backend
pip install -r requirements.txt
python -m uvicorn app.main:app --reload
```

Expected: 服务正常启动在 http://localhost:8000，访问 http://localhost:8000/docs 看到Swagger文档

- [ ] **Step 3: 确保用户端前端可正常启动**

```bash
cd apps/frontend-user
npm install
npm run dev
```

Expected: 前端正常启动在 http://localhost:3000

- [ ] **Step 4: 确保管理端前端可正常启动**

```bash
cd apps/frontend-admin
npm install
npm run dev
```

Expected: 管理端正常启动在 http://localhost:3001

- [ ] **Step 5: 安装Playwright浏览器驱动**

```bash
cd apps/backend
pip install playwright
playwright install chromium
```

Expected: Chromium浏览器安装成功

---

## 第1层：单元测试执行与扩展

### Task 1: 运行现有单元测试，修复失败用例

**Files:**
- Run: `apps/backend/tests/test_auth_service.py`
- Run: `apps/backend/tests/test_user_service.py`
- Run: `apps/backend/tests/test_point_service.py`
- Run: `apps/backend/tests/test_security.py`

- [ ] **Step 1: 运行所有单元测试**

```bash
cd apps/backend
pytest tests/ -v --cov=app --cov-report=html
```

- [ ] **Step 2: 记录失败用例，分类统计**

记录所有失败测试到：`docs/test-reports/单元测试失败记录.md`
包含：测试名、失败原因、错误堆栈、分级

- [ ] **Step 3: 修复P0/P1级别失败用例**

对于核心逻辑失败，通知后端子代理修复，修复后重新运行验证

- [ ] **Step 4: 生成单元测试覆盖率报告**

Expected: 覆盖率 > 80%，核心模块 > 90%

- [ ] **Step 5: Commit测试结果和修复代码**

```bash
git add apps/backend/tests/
git add docs/test-reports/
git commit -m "test: 单元测试执行与问题修复"
```

---

### Task 2: 扩展单元测试用例，提升覆盖率到90%+

**Files:**
- Create/Modify: `apps/backend/tests/test_auth_service.py`
- Create/Modify: `apps/backend/tests/test_user_service.py`
- Create/Modify: `apps/backend/tests/test_point_service.py`

- [ ] **Step 1: 扩展Auth Service测试用例**

增加以下测试到 `test_auth_service.py`：

```python
def test_login_with_wrong_password():
    """测试使用错误密码登录"""
    result = auth_service.login(username="testuser", password="wrongpassword")
    assert result.success is False
    assert "密码错误" in result.message

def test_login_with_non_existent_user():
    """测试不存在的用户登录"""
    result = auth_service.login(username="nonexistent", password="anypass")
    assert result.success is False

def test_token_expired_verification():
    """测试过期Token验证"""
    expired_token = create_expired_token()
    result = security_service.verify_token(expired_token)
    assert result.valid is False

def test_refresh_token():
    """测试Token刷新"""
    token = create_test_token()
    new_token = auth_service.refresh_token(token)
    assert new_token is not None
    assert new_token != token
```

- [ ] **Step 2: 扩展User Service测试用例**

增加以下测试到 `test_user_service.py`：

```python
def test_update_user_info():
    """测试更新用户信息"""
    user = create_test_user()
    updated = user_service.update_info(user.id, {"nickname": "新昵称"})
    assert updated.nickname == "新昵称"

def test_real_name_verification():
    """测试实名认证"""
    user = create_test_user()
    result = user_service.verify_real_name(user.id, "张三", "110101199001011234")
    assert result.success is True
    assert user.is_verified is True

def test_disable_user():
    """测试禁用用户"""
    user = create_test_user()
    user_service.disable(user.id)
    updated = user_service.get_by_id(user.id)
    assert updated.status == "disabled"
```

- [ ] **Step 3: 扩展Point Service测试用例**

增加以下测试到 `test_point_service.py`：

```python
def test_point_freeze_and_settle():
    """测试积分预冻结与结算"""
    user = create_test_user()
    initial_balance = user.point_balance
    
    # 预冻结
    freeze_result = point_service.freeze(user.id, 50, "测试冻结")
    assert freeze_result.success is True
    assert user.point_balance == initial_balance - 50
    assert user.frozen_points == 50
    
    # 结算扣款
    settle_result = point_service.settle(freeze_result.freeze_id)
    assert settle_result.success is True

def test_point_concurrent_operations():
    """测试并发积分操作（乐观锁验证）"""
    import asyncio
    user = create_test_user()
    initial_balance = user.point_balance
    
    async def add_points():
        await point_service.add(user.id, 10, "并发测试")
    
    # 并发执行10次增加操作
    asyncio.run(asyncio.gather(*[add_points() for _ in range(10)]))
    
    updated = user_service.get_by_id(user.id)
    assert updated.point_balance == initial_balance + 100
```

- [ ] **Step 4: 运行扩展后的测试，验证全部通过**

```bash
cd apps/backend
pytest tests/ -v --cov=app
```

Expected: 所有测试通过，覆盖率 > 90%

- [ ] **Step 5: Commit新增测试用例**

```bash
git add apps/backend/tests/
git commit -m "test: 扩展单元测试用例，覆盖率提升至90%+"
```

---

## 第2层：接口测试开发与执行

### Task 3: 开发认证接口测试用例

**Files:**
- Create: `apps/backend/tests/test_api_auth.py`

- [ ] **Step 1: 创建认证接口测试文件**

写入 `apps/backend/tests/test_api_auth.py`：

```python
import pytest
from httpx import AsyncClient

@pytest.mark.asyncio
async def test_login_success(client: AsyncClient):
    """测试登录成功"""
    response = await client.post(
        "/api/v1/auth/login",
        json={"username": "admin", "password": "admin123"}
    )
    assert response.status_code == 200
    data = response.json()
    assert "access_token" in data
    assert data["token_type"] == "bearer"

@pytest.mark.asyncio
async def test_login_wrong_password(client: AsyncClient):
    """测试错误密码登录"""
    response = await client.post(
        "/api/v1/auth/login",
        json={"username": "admin", "password": "wrongpass"}
    )
    assert response.status_code == 401

@pytest.mark.asyncio
async def test_register_new_user(client: AsyncClient):
    """测试新用户注册"""
    response = await client.post(
        "/api/v1/auth/register",
        json={
            "username": "newtestuser",
            "email": "newtest@test.com",
            "password": "test123456",
            "verify_code": "8888"
        }
    )
    assert response.status_code in [200, 201]

@pytest.mark.asyncio
async def test_register_duplicate_username(client: AsyncClient):
    """测试重复用户名注册"""
    # 第一次注册
    await client.post(
        "/api/v1/auth/register",
        json={
            "username": "duplicateuser",
            "email": "dup1@test.com",
            "password": "test123456",
            "verify_code": "8888"
        }
    )
    # 第二次使用相同用户名
    response = await client.post(
        "/api/v1/auth/register",
        json={
            "username": "duplicateuser",
            "email": "dup2@test.com",
            "password": "test123456",
            "verify_code": "8888"
        }
    )
    assert response.status_code == 400

@pytest.mark.asyncio
async def test_logout(client: AsyncClient):
    """测试登出"""
    # 先登录获取token
    login_resp = await client.post(
        "/api/v1/auth/login",
        json={"username": "admin", "password": "admin123"}
    )
    token = login_resp.json()["access_token"]
    
    response = await client.post(
        "/api/v1/auth/logout",
        headers={"Authorization": f"Bearer {token}"}
    )
    assert response.status_code in [200, 204]

@pytest.mark.asyncio
async def test_unauthorized_access(client: AsyncClient):
    """测试未授权访问"""
    response = await client.get("/api/v1/users/me")
    assert response.status_code == 401
```

- [ ] **Step 2: 运行认证接口测试**

```bash
cd apps/backend
pytest tests/test_api_auth.py -v
```

- [ ] **Step 3: 记录并修复失败用例**

- [ ] **Step 4: Commit认证接口测试**

```bash
git add apps/backend/tests/test_api_auth.py
git commit -m "test: 认证接口测试用例开发"
```

---

### Task 4: 开发用户接口测试用例

**Files:**
- Create: `apps/backend/tests/test_api_user.py`

- [ ] **Step 1: 创建用户接口测试文件**

写入 `apps/backend/tests/test_api_user.py`：

```python
import pytest
from httpx import AsyncClient

@pytest.fixture
async def auth_headers(client: AsyncClient):
    """获取已认证请求头"""
    response = await client.post(
        "/api/v1/auth/login",
        json={"username": "testuser", "password": "test123"}
    )
    token = response.json()["access_token"]
    return {"Authorization": f"Bearer {token}"}

@pytest.mark.asyncio
async def test_get_current_user_info(client: AsyncClient, auth_headers):
    """测试获取当前用户信息"""
    response = await client.get("/api/v1/users/me", headers=auth_headers)
    assert response.status_code == 200
    data = response.json()
    assert data["username"] == "testuser"

@pytest.mark.asyncio
async def test_update_user_info(client: AsyncClient, auth_headers):
    """测试更新用户信息"""
    response = await client.put(
        "/api/v1/users/me",
        headers=auth_headers,
        json={"nickname": "测试昵称", "avatar": "http://test.com/avatar.png"}
    )
    assert response.status_code == 200
    data = response.json()
    assert data["nickname"] == "测试昵称"

@pytest.mark.asyncio
async def test_submit_real_name_verification(client: AsyncClient, auth_headers):
    """测试提交实名认证"""
    response = await client.post(
        "/api/v1/users/me/verify",
        headers=auth_headers,
        json={
            "real_name": "张三",
            "id_card": "110101199001011234"
        }
    )
    assert response.status_code == 200

@pytest.mark.asyncio
async def test_change_password(client: AsyncClient, auth_headers):
    """测试修改密码"""
    response = await client.post(
        "/api/v1/users/me/change-password",
        headers=auth_headers,
        json={
            "old_password": "test123",
            "new_password": "newtest123"
        }
    )
    assert response.status_code == 200
    
    # 验证新密码可以登录
    login_resp = await client.post(
        "/api/v1/auth/login",
        json={"username": "testuser", "password": "newtest123"}
    )
    assert login_resp.status_code == 200
```

- [ ] **Step 2: 运行用户接口测试**

```bash
cd apps/backend
pytest tests/test_api_user.py -v
```

- [ ] **Step 3: 记录并修复失败用例**

- [ ] **Step 4: Commit用户接口测试**

```bash
git add apps/backend/tests/test_api_user.py
git commit -m "test: 用户接口测试用例开发"
```

---

### Task 5: 开发积分接口测试用例

**Files:**
- Create: `apps/backend/tests/test_api_point.py`

- [ ] **Step 1: 创建积分接口测试文件**

写入 `apps/backend/tests/test_api_point.py`：

```python
import pytest
from httpx import AsyncClient

@pytest.fixture
async def auth_headers(client: AsyncClient):
    """获取已认证请求头"""
    response = await client.post(
        "/api/v1/auth/login",
        json={"username": "testuser", "password": "test123"}
    )
    token = response.json()["access_token"]
    return {"Authorization": f"Bearer {token}"}

@pytest.mark.asyncio
async def test_get_point_balance(client: AsyncClient, auth_headers):
    """测试获取积分余额"""
    response = await client.get("/api/v1/points/balance", headers=auth_headers)
    assert response.status_code == 200
    data = response.json()
    assert "balance" in data

@pytest.mark.asyncio
async def test_get_point_history(client: AsyncClient, auth_headers):
    """测试获取积分流水"""
    response = await client.get("/api/v1/points/history?page=1&page_size=10", headers=auth_headers)
    assert response.status_code == 200
    data = response.json()
    assert "items" in data
    assert "total" in data

@pytest.mark.asyncio
async def test_point_recharge(client: AsyncClient, auth_headers):
    """测试积分充值"""
    # 先获取当前余额
    balance_resp = await client.get("/api/v1/points/balance", headers=auth_headers)
    initial_balance = balance_resp.json()["balance"]
    
    # 执行充值
    response = await client.post(
        "/api/v1/points/recharge",
        headers=auth_headers,
        json={"amount": 100, "payment_method": "alipay"}
    )
    assert response.status_code == 200
    
    # 验证余额增加
    new_balance_resp = await client.get("/api/v1/points/balance", headers=auth_headers)
    new_balance = new_balance_resp.json()["balance"]
    assert new_balance == initial_balance + 100
```

- [ ] **Step 2: 运行积分接口测试**

```bash
cd apps/backend
pytest tests/test_api_point.py -v
```

- [ ] **Step 3: 记录并修复失败用例**

- [ ] **Step 4: Commit积分接口测试**

```bash
git add apps/backend/tests/test_api_point.py
git commit -m "test: 积分接口测试用例开发"
```

---

### Task 6: 开发管理员接口测试用例

**Files:**
- Create: `apps/backend/tests/test_api_admin.py`

- [ ] **Step 1: 创建管理员接口测试文件**

写入 `apps/backend/tests/test_api_admin.py`：

```python
import pytest
from httpx import AsyncClient

@pytest.fixture
async def admin_auth_headers(client: AsyncClient):
    """获取管理员认证请求头"""
    response = await client.post(
        "/api/v1/auth/login",
        json={"username": "admin", "password": "admin123"}
    )
    token = response.json()["access_token"]
    return {"Authorization": f"Bearer {token}"}

@pytest.fixture
async def user_auth_headers(client: AsyncClient):
    """获取普通用户认证请求头"""
    response = await client.post(
        "/api/v1/auth/login",
        json={"username": "testuser", "password": "test123"}
    )
    token = response.json()["access_token"]
    return {"Authorization": f"Bearer {token}"}

@pytest.mark.asyncio
async def test_admin_get_user_list(client: AsyncClient, admin_auth_headers):
    """测试管理员获取用户列表"""
    response = await client.get(
        "/api/v1/admin/users?page=1&page_size=10",
        headers=admin_auth_headers
    )
    assert response.status_code == 200
    data = response.json()
    assert "items" in data
    assert "total" in data

@pytest.mark.asyncio
async def test_user_cannot_access_admin_api(client: AsyncClient, user_auth_headers):
    """测试普通用户无法访问管理员接口"""
    response = await client.get("/api/v1/admin/users", headers=user_auth_headers)
    assert response.status_code == 403

@pytest.mark.asyncio
async def test_admin_disable_user(client: AsyncClient, admin_auth_headers):
    """测试管理员禁用用户"""
    response = await client.post(
        "/api/v1/admin/users/1/disable",
        headers=admin_auth_headers
    )
    assert response.status_code == 200

@pytest.mark.asyncio
async def test_admin_adjust_user_points(client: AsyncClient, admin_auth_headers):
    """测试管理员调整用户积分"""
    response = await client.post(
        "/api/v1/admin/users/1/points",
        headers=admin_auth_headers,
        json={"amount": 500, "reason": "管理员调整测试"}
    )
    assert response.status_code == 200
```

- [ ] **Step 2: 运行管理员接口测试**

```bash
cd apps/backend
pytest tests/test_api_admin.py -v
```

- [ ] **Step 3: 记录并修复失败用例**

- [ ] **Step 4: Commit管理员接口测试**

```bash
git add apps/backend/tests/test_api_admin.py
git commit -m "test: 管理员接口测试用例开发"
```

---

### Task 7: 接口字段一致性专项检查

**Files:**
- Check: `apps/frontend-user/src/lib/api.ts`
- Check: `apps/frontend-admin/src/api/`
- Check: Swagger docs at `http://localhost:8000/docs`

- [ ] **Step 1: 提取前端API定义的所有字段**

```bash
# 扫描前端API文件，提取所有请求/响应字段
cd apps/frontend-user
grep -n "interface\|type\|{" src/lib/api.ts > frontend_fields.txt
```

- [ ] **Step 2: 提取后端Swagger文档的所有字段**

访问 http://localhost:8000/docs，导出OpenAPI JSON并提取所有字段定义

- [ ] **Step 3: 逐项比对字段一致性**

创建字段映射核对表：`docs/test-reports/前后端字段一致性核对表.md`

| 接口名称 | 前端字段 | 后端字段 | 是否一致 | 备注 |
|---------|---------|---------|---------|------|
| 登录请求 | username | username | ✅ | |
| 登录请求 | password | password | ✅ | |
| ... | ... | ... | ... | ... |

- [ ] **Step 4: 修复所有字段不一致问题**

对于不一致的字段，通知对应子代理修复（前端/后端）

- [ ] **Step 5: Commit核对报告和修复代码**

```bash
git add docs/test-reports/前后端字段一致性核对表.md
git commit -m "test: 前后端字段一致性检查完成"
```

---

## 第3层：E2E自动化测试开发与执行

### Task 8: 用户注册流程E2E测试

**Files:**
- Create/Modify: `apps/backend/tests/e2e/test_e2e_register.py`

- [ ] **Step 1: 编写注册流程测试**

写入 `apps/backend/tests/e2e/test_e2e_register.py`：

```python
import pytest
from playwright.sync_api import Page, expect
import os
import time

SCREENSHOTS_DIR = "tests/e2e/screenshots/register"
os.makedirs(SCREENSHOTS_DIR, exist_ok=True)

BASE_URL = os.getenv("E2E_BASE_URL", "http://localhost:3000")

class TestRegisterFlow:
    """用户注册流程测试"""
    
    def test_access_register_page(self, page: Page):
        """测试访问注册页面"""
        page.goto(f"{BASE_URL}/register")
        expect(page).to_have_url(f"{BASE_URL}/register")
        page.screenshot(path=f"{SCREENSHOTS_DIR}/01_register_page_loaded.png")
        
        # 验证页面元素
        expect(page.get_by_role("heading", name="注册账号")).to_be_visible()
        expect(page.get_by_placeholder("请输入用户名")).to_be_visible()
        expect(page.get_by_placeholder("请输入邮箱")).to_be_visible()
        expect(page.get_by_placeholder("请输入密码", exact=True)).to_be_visible()
        expect(page.get_by_placeholder("确认密码")).to_be_visible()
        expect(page.get_by_role("button", name="注册")).to_be_visible()
        page.screenshot(path=f"{SCREENSHOTS_DIR}/02_register_form_elements.png")
    
    def test_form_validation(self, page: Page):
        """测试表单验证"""
        page.goto(f"{BASE_URL}/register")
        
        # 密码显示/隐藏切换
        password_input = page.get_by_placeholder("请输入密码", exact=True)
        password_input.fill("test123456")
        page.screenshot(path=f"{SCREENSHOTS_DIR}/03_password_hidden.png")
        
        # 点击显示密码按钮
        show_btn = page.locator(".password-toggle-btn")
        if show_btn.is_visible():
            show_btn.click()
            page.screenshot(path=f"{SCREENSHOTS_DIR}/04_password_shown.png")
    
    def test_navigate_to_login(self, page: Page):
        """测试从注册页跳转到登录页"""
        page.goto(f"{BASE_URL}/register")
        page.screenshot(path=f"{SCREENSHOTS_DIR}/05_before_navigate_to_login.png")
        
        # 点击"已有账号？去登录"链接
        page.get_by_text("已有账号").click()
        expect(page).to_have_url(f"{BASE_URL}/login")
        page.screenshot(path=f"{SCREENSHOTS_DIR}/06_navigated_to_login.png")
    
    def test_register_success(self, page: Page):
        """测试注册成功流程"""
        import random
        random_str = str(random.randint(1000, 9999))
        username = f"e2etestuser_{random_str}"
        email = f"{username}@test.com"
        
        page.goto(f"{BASE_URL}/register")
        
        # 填写表单
        page.get_by_placeholder("请输入用户名").fill(username)
        page.get_by_placeholder("请输入邮箱").fill(email)
        page.get_by_placeholder("请输入密码", exact=True).fill("Test123456!")
        page.get_by_placeholder("确认密码").fill("Test123456!")
        page.get_by_placeholder("验证码").fill("8888")
        
        page.screenshot(path=f"{SCREENSHOTS_DIR}/07_register_form_filled.png")
        
        # 提交注册
        page.get_by_role("button", name="注册").click()
        
        # 验证跳转到登录页或首页
        page.wait_for_url(regex=rf"({BASE_URL}/login|{BASE_URL}/)", timeout=10000)
        page.screenshot(path=f"{SCREENSHOTS_DIR}/08_register_success.png")
```

- [ ] **Step 2: 运行注册流程E2E测试（可见模式）**

```bash
cd apps/backend
E2E_HEADLESS=false pytest tests/e2e/test_e2e_register.py -v
```

- [ ] **Step 3: 查看截图，验证页面显示符合设计规范**

检查 `apps/backend/tests/e2e/screenshots/register/` 下的所有截图

- [ ] **Step 4: 修复发现的问题并复测**

- [ ] **Step 5: Commit注册测试代码和截图**

```bash
git add apps/backend/tests/e2e/test_e2e_register.py
git add apps/backend/tests/e2e/screenshots/register/
git commit -m "test: 用户注册流程E2E测试完成"
```

---

### Task 9: 用户登录流程E2E测试

**Files:**
- Create/Modify: `apps/backend/tests/e2e/test_e2e_login.py`

- [ ] **Step 1: 编写登录流程测试**

写入 `apps/backend/tests/e2e/test_e2e_login.py`：

```python
import pytest
from playwright.sync_api import Page, expect
import os

SCREENSHOTS_DIR = "tests/e2e/screenshots/login"
os.makedirs(SCREENSHOTS_DIR, exist_ok=True)

BASE_URL = os.getenv("E2E_BASE_URL", "http://localhost:3000")

class TestLoginFlow:
    """用户登录流程测试"""
    
    def test_access_login_page(self, page: Page):
        """测试访问登录页面"""
        page.goto(f"{BASE_URL}/login")
        expect(page).to_have_url(f"{BASE_URL}/login")
        page.screenshot(path=f"{SCREENSHOTS_DIR}/01_login_page_loaded.png")
        
        # 验证页面元素
        expect(page.get_by_role("heading", name="登录账号")).to_be_visible()
        expect(page.get_by_placeholder("请输入用户名")).to_be_visible()
        expect(page.get_by_placeholder("请输入密码")).to_be_visible()
        expect(page.get_by_role("button", name="登录")).to_be_visible()
        page.screenshot(path=f"{SCREENSHOTS_DIR}/02_login_form_elements.png")
    
    def test_login_success(self, page: Page):
        """测试登录成功"""
        page.goto(f"{BASE_URL}/login")
        
        # 填写登录信息
        page.get_by_placeholder("请输入用户名").fill("testuser")
        page.screenshot(path=f"{SCREENSHOTS_DIR}/03_username_filled.png")
        
        page.get_by_placeholder("请输入密码").fill("test123")
        page.screenshot(path=f"{SCREENSHOTS_DIR}/04_password_filled.png")
        
        # 点击登录
        page.get_by_role("button", name="登录").click()
        
        # 验证跳转到首页或用户中心
        page.wait_for_url(regex=rf"({BASE_URL}/|{BASE_URL}/user-center)", timeout=10000)
        page.screenshot(path=f"{SCREENSHOTS_DIR}/05_login_success.png")
        
        # 验证用户信息显示
        expect(page.locator(".user-avatar, .user-name")).to_be_visible()
    
    def test_login_wrong_password(self, page: Page):
        """测试错误密码登录"""
        page.goto(f"{BASE_URL}/login")
        
        page.get_by_placeholder("请输入用户名").fill("testuser")
        page.get_by_placeholder("请输入密码").fill("wrongpassword")
        page.get_by_role("button", name="登录").click()
        
        # 验证错误提示
        expect(page.get_by_text("密码错误|用户名或密码错误")).to_be_visible(timeout=5000)
        page.screenshot(path=f"{SCREENSHOTS_DIR}/06_login_failed.png")
    
    def test_navigate_to_register(self, page: Page):
        """测试从登录页跳转到注册页"""
        page.goto(f"{BASE_URL}/login")
        page.get_by_text("没有账号|立即注册").click()
        expect(page).to_have_url(f"{BASE_URL}/register")
        page.screenshot(path=f"{SCREENSHOTS_DIR}/07_navigated_to_register.png")
    
    def test_logout_flow(self, page: Page, logged_in_page):
        """测试登出流程"""
        logged_in_page.goto(f"{BASE_URL}/user-center")
        logged_in_page.screenshot(path=f"{SCREENSHOTS_DIR}/08_before_logout.png")
        
        # 点击退出登录
        logged_in_page.get_by_text("退出登录|登出").click()
        
        # 确认登出（如果有确认弹窗）
        if logged_in_page.get_by_text("确认|确定").is_visible():
            logged_in_page.get_by_text("确认|确定").click()
        
        # 验证回到首页/登录页
        logged_in_page.wait_for_url(regex=rf"({BASE_URL}/|{BASE_URL}/login)", timeout=10000)
        logged_in_page.screenshot(path=f"{SCREENSHOTS_DIR}/09_logout_success.png")
```

- [ ] **Step 2: 运行登录流程E2E测试（可见模式）**

```bash
cd apps/backend
E2E_HEADLESS=false pytest tests/e2e/test_e2e_login.py -v
```

- [ ] **Step 3: 查看截图，验证登录流程顺畅**

- [ ] **Step 4: 修复发现的问题并复测**

- [ ] **Step 5: Commit登录测试代码和截图**

```bash
git add apps/backend/tests/e2e/test_e2e_login.py
git add apps/backend/tests/e2e/screenshots/login/
git commit -m "test: 用户登录流程E2E测试完成"
```

---

### Task 10: 实名认证流程E2E测试

**Files:**
- Create: `apps/backend/tests/e2e/test_e2e_verification.py`

- [ ] **Step 1: 编写实名认证流程测试**

写入 `apps/backend/tests/e2e/test_e2e_verification.py`：

```python
import pytest
from playwright.sync_api import Page, expect
import os

SCREENSHOTS_DIR = "tests/e2e/screenshots/verification"
os.makedirs(SCREENSHOTS_DIR, exist_ok=True)

BASE_URL = os.getenv("E2E_BASE_URL", "http://localhost:3000")

class TestVerificationFlow:
    """实名认证流程测试"""
    
    def test_access_verification_page(self, logged_in_page):
        """测试访问实名认证页面"""
        logged_in_page.goto(f"{BASE_URL}/verification")
        logged_in_page.screenshot(path=f"{SCREENSHOTS_DIR}/01_verification_page_loaded.png")
        
        # 验证页面元素
        expect(logged_in_page.get_by_role("heading", name="实名认证|实名验证")).to_be_visible()
        expect(logged_in_page.get_by_placeholder("请输入真实姓名")).to_be_visible()
        expect(logged_in_page.get_by_placeholder("请输入身份证号")).to_be_visible()
        expect(logged_in_page.get_by_role("button", name="提交|立即认证")).to_be_visible()
    
    def test_submit_verification_success(self, logged_in_page):
        """测试提交实名认证成功"""
        logged_in_page.goto(f"{BASE_URL}/verification")
        
        # 填写信息
        logged_in_page.get_by_placeholder("请输入真实姓名").fill("张三")
        logged_in_page.get_by_placeholder("请输入身份证号").fill("110101199001011234")
        logged_in_page.screenshot(path=f"{SCREENSHOTS_DIR}/02_verification_form_filled.png")
        
        # 勾选同意协议（如果有）
        checkbox = logged_in_page.locator("input[type='checkbox']")
        if checkbox.is_visible():
            checkbox.check()
        
        # 提交
        logged_in_page.get_by_role("button", name="提交|立即认证").click()
        
        # 验证提交成功提示
        expect(logged_in_page.get_by_text("提交成功|认证成功|审核中")).to_be_visible(timeout=10000)
        logged_in_page.screenshot(path=f"{SCREENSHOTS_DIR}/03_verification_submitted.png")
```

- [ ] **Step 2: 运行实名认证E2E测试（可见模式）**

```bash
cd apps/backend
E2E_HEADLESS=false pytest tests/e2e/test_e2e_verification.py -v
```

- [ ] **Step 3: 修复问题并Commit**

```bash
git add apps/backend/tests/e2e/test_e2e_verification.py
git commit -m "test: 实名认证流程E2E测试完成"
```

---

### Task 11: 积分充值与个人中心E2E测试

**Files:**
- Create: `apps/backend/tests/e2e/test_e2e_point.py`
- Create: `apps/backend/tests/e2e/test_e2e_user_center.py`

- [ ] **Step 1: 编写积分充值测试**

写入 `apps/backend/tests/e2e/test_e2e_point.py`：

```python
import pytest
from playwright.sync_api import Page, expect
import os

SCREENSHOTS_DIR = "tests/e2e/screenshots/point"
os.makedirs(SCREENSHOTS_DIR, exist_ok=True)

BASE_URL = os.getenv("E2E_BASE_URL", "http://localhost:3000")

class TestPointFlow:
    """积分充值流程测试"""
    
    def test_view_point_balance(self, logged_in_page):
        """测试查看积分余额"""
        logged_in_page.goto(f"{BASE_URL}/user-center")
        # 查找余额显示
        expect(logged_in_page.locator(".point-balance, .balance")).to_be_visible()
        logged_in_page.screenshot(path=f"{SCREENSHOTS_DIR}/01_balance_display.png")
    
    def test_recharge_page(self, logged_in_page):
        """测试充值页面"""
        logged_in_page.goto(f"{BASE_URL}/recharge")
        
        # 验证充值档位显示
        expect(logged_in_page.locator(".recharge-package, .price-item")).to_have_count(greater_than=0)
        logged_in_page.screenshot(path=f"{SCREENSHOTS_DIR}/02_recharge_page.png")
        
        # 选择一个档位
        logged_in_page.locator(".recharge-package").first.click()
        logged_in_page.screenshot(path=f"{SCREENSHOTS_DIR}/03_package_selected.png")
```

- [ ] **Step 2: 编写个人中心测试**

写入 `apps/backend/tests/e2e/test_e2e_user_center.py`：

```python
import pytest
from playwright.sync_api import Page, expect
import os

SCREENSHOTS_DIR = "tests/e2e/screenshots/user_center"
os.makedirs(SCREENSHOTS_DIR, exist_ok=True)

BASE_URL = os.getenv("E2E_BASE_URL", "http://localhost:3000")

class TestUserCenterFlow:
    """个人中心流程测试"""
    
    def test_view_user_info(self, logged_in_page):
        """测试查看用户信息"""
        logged_in_page.goto(f"{BASE_URL}/user-center")
        expect(logged_in_page.locator(".user-info, .profile")).to_be_visible()
        logged_in_page.screenshot(path=f"{SCREENSHOTS_DIR}/01_user_info.png")
    
    def test_edit_user_info(self, logged_in_page):
        """测试编辑用户信息"""
        logged_in_page.goto(f"{BASE_URL}/user-center/edit")
        
        # 修改昵称
        nickname_input = logged_in_page.get_by_placeholder("昵称|请输入昵称")
        if nickname_input.is_visible():
            nickname_input.fill("E2E测试用户")
            logged_in_page.screenshot(path=f"{SCREENSHOTS_DIR}/02_edit_info.png")
            
            # 保存
            logged_in_page.get_by_role("button", name="保存|提交").click()
            expect(logged_in_page.get_by_text("保存成功|修改成功")).to_be_visible(timeout=5000)
            logged_in_page.screenshot(path=f"{SCREENSHOTS_DIR}/03_edit_success.png")
    
    def test_view_point_history(self, logged_in_page):
        """测试查看积分流水"""
        logged_in_page.goto(f"{BASE_URL}/user-center/points")
        expect(logged_in_page.locator(".point-list, .history-list")).to_be_visible()
        logged_in_page.screenshot(path=f"{SCREENSHOTS_DIR}/04_point_history.png")
```

- [ ] **Step 3: 运行积分和个人中心E2E测试**

```bash
cd apps/backend
E2E_HEADLESS=false pytest tests/e2e/test_e2e_point.py tests/e2e/test_e2e_user_center.py -v
```

- [ ] **Step 4: Commit测试代码和截图**

```bash
git add apps/backend/tests/e2e/test_e2e_point.py
git add apps/backend/tests/e2e/test_e2e_user_center.py
git commit -m "test: 积分充值与个人中心E2E测试完成"
```

---

### Task 12: 管理端用户管理E2E测试

**Files:**
- Create: `apps/backend/tests/e2e/test_e2e_admin.py`

- [ ] **Step 1: 编写管理端用户管理测试**

写入 `apps/backend/tests/e2e/test_e2e_admin.py`：

```python
import pytest
from playwright.sync_api import Page, expect
import os

SCREENSHOTS_DIR = "tests/e2e/screenshots/admin"
os.makedirs(SCREENSHOTS_DIR, exist_ok=True)

ADMIN_BASE_URL = "http://localhost:3001"

class TestAdminUserManagement:
    """管理端用户管理测试"""
    
    def test_admin_login(self, page: Page):
        """测试管理员登录"""
        page.goto(f"{ADMIN_BASE_URL}/login")
        page.screenshot(path=f"{SCREENSHOTS_DIR}/01_admin_login_page.png")
        
        page.get_by_placeholder("请输入用户名").fill("admin")
        page.get_by_placeholder("请输入密码").fill("admin123")
        page.get_by_role("button", name="登录").click()
        
        page.wait_for_url(regex=rf"{ADMIN_BASE_URL}/(dashboard|)?", timeout=10000)
        page.screenshot(path=f"{SCREENSHOTS_DIR}/02_admin_logged_in.png")
    
    def test_view_user_list(self, page: Page):
        """测试查看用户列表"""
        # 先登录
        page.goto(f"{ADMIN_BASE_URL}/login")
        page.get_by_placeholder("请输入用户名").fill("admin")
        page.get_by_placeholder("请输入密码").fill("admin123")
        page.get_by_role("button", name="登录").click()
        page.wait_for_url(regex=rf"{ADMIN_BASE_URL}/(dashboard|)?", timeout=10000)
        
        # 访问用户管理
        page.goto(f"{ADMIN_BASE_URL}/users")
        page.screenshot(path=f"{SCREENSHOTS_DIR}/03_user_list.png")
        
        # 验证表格显示
        expect(page.locator("table, .ant-table, .user-table")).to_be_visible()
    
    def test_disable_user(self, page: Page):
        """测试禁用用户"""
        # 先登录...
        page.goto(f"{ADMIN_BASE_URL}/users")
        page.screenshot(path=f"{SCREENSHOTS_DIR}/04_before_disable.png")
        
        # 点击禁用按钮
        disable_btn = page.get_by_role("button", name="禁用").first
        if disable_btn.is_visible():
            disable_btn.click()
            
            # 确认弹窗
            if page.get_by_text("确认|确定").is_visible():
                page.get_by_text("确认|确定").click()
            
            expect(page.get_by_text("操作成功|禁用成功")).to_be_visible(timeout=5000)
            page.screenshot(path=f"{SCREENSHOTS_DIR}/05_user_disabled.png")
```

- [ ] **Step 2: 运行管理端E2E测试（可见模式）**

```bash
cd apps/backend
E2E_HEADLESS=false pytest tests/e2e/test_e2e_admin.py -v
```

- [ ] **Step 3: Commit测试代码和截图**

```bash
git add apps/backend/tests/e2e/test_e2e_admin.py
git commit -m "test: 管理端用户管理E2E测试完成"
```

---

## 第4层：手工验证与Mock接口清理

### Task 13: Mock接口清理专项检查

**Files:**
- Check: `apps/frontend-user/src/lib/api.ts`
- Check: `apps/frontend-admin/src/api/`
- Grep: `apps/**/*.tsx`, `apps/**/*.ts` for mock/fake/static

- [ ] **Step 1: 全局搜索Mock数据关键字**

```bash
cd apps

# 搜索mock相关关键字
grep -r -i "mock\|fake\|static.*data\|mockData\|mock_data" --include="*.ts" --include="*.tsx" frontend-user/src/ > ../docs/test-reports/mock_search_result.txt
grep -r -i "mock\|fake\|static.*data\|mockData\|mock_data" --include="*.ts" --include="*.tsx" frontend-admin/src/ >> ../docs/test-reports/mock_search_result.txt
```

- [ ] **Step 2: 逐项检查每个匹配项，确认是否为硬编码假数据**

创建报告：`docs/test-reports/Mock接口清理报告.md`

| 文件 | 位置 | 内容 | 是否Mock | 处理状态 |
|-----|------|------|----------|----------|
| api.ts | 第45行 | const mockUser = {...} | ✅ 是 | 待替换 |
| ... | ... | ... | ... | ... |

- [ ] **Step 3: 替换所有Mock数据为真实后端接口调用**

对于每个发现的Mock数据，通知前端子代理替换为真实API调用

- [ ] **Step 4: 验证所有API调用都指向真实后端**

```bash
# 检查API基础URL配置
grep -r "baseURL\|BASE_URL\|localhost:8000" apps/frontend-user/src/lib/api.ts
grep -r "baseURL\|BASE_URL\|localhost:8000" apps/frontend-admin/src/api/
```

Expected: 所有API调用都指向 http://localhost:8000 或配置的真实后端地址

- [ ] **Step 5: Commit Mock清理报告和代码修复**

```bash
git add docs/test-reports/Mock接口清理报告.md
git commit -m "test: Mock接口清理完成，全部对接真实后端"
```

---

### Task 14: 异常场景手工验证

**Files:**
- Manual testing on frontend and backend

- [ ] **Step 1: Token过期场景测试**

1. 正常登录
2. 修改localStorage中的Token为过期值（或等待Token过期）
3. 刷新页面
4. 验证：自动跳转登录页，提示登录已过期

- [ ] **Step 2: Token篡改场景测试**

1. 正常登录
2. 修改localStorage中的Token字符串（篡改几个字符）
3. 刷新页面
4. 验证：自动跳转登录页

- [ ] **Step 3: 权限绕过场景测试**

1. 使用普通用户登录
2. 直接在地址栏输入管理员页面URL（如 http://localhost:3001/users）
3. 验证：跳转到403页面或无权限提示

- [ ] **Step 4: 并发操作场景测试**

1. 打开两个浏览器窗口，使用同一个账号登录
2. 同时进行积分消费操作
3. 验证：数据正确，不出现超扣或重复消费

- [ ] **Step 5: 记录所有测试结果**

写入 `docs/test-reports/异常场景手工测试报告.md`

- [ ] **Step 6: Commit测试报告**

```bash
git add docs/test-reports/异常场景手工测试报告.md
git commit -m "test: 异常场景手工验证完成"
```

---

### Task 15: 全量回归测试与问题闭环

**Files:**
- All test files

- [ ] **Step 1: 运行所有单元测试**

```bash
cd apps/backend
pytest tests/test_*.py -v --cov=app --cov-report=html
```

Expected: 通过率 = 100%

- [ ] **Step 2: 运行所有接口测试**

```bash
cd apps/backend
pytest tests/test_api_*.py -v
```

Expected: 通过率 = 100%

- [ ] **Step 3: 运行所有E2E测试（无头模式）**

```bash
cd apps/backend
pytest tests/e2e/ -v --html=tests/e2e/reports/e2e_report.html
```

Expected: 核心流程通过率 = 100%

- [ ] **Step 4: 更新问题追踪表，确认所有P0/P1问题已闭环**

更新 `docs/test-reports/问题追踪表.md`，确认：
- P0问题：100%闭环
- P1问题：100%闭环
- P2问题：≥90%闭环

- [ ] **Step 5: Commit回归测试结果**

```bash
git add apps/backend/tests/e2e/reports/
git add docs/test-reports/问题追踪表.md
git commit -m "test: 全量回归测试完成，问题全部闭环"
```

---

### Task 16: 生成验收级测试报告

**Files:**
- Create: `docs/test-reports/阶段一核心用户系统验收测试报告.md`

- [ ] **Step 1: 编写验收测试报告**

报告包含以下内容：

```markdown
# 阶段一核心用户系统 - 验收测试报告

## 测试概览

- **测试时间**：2026-05-19
- **测试范围**：核心用户系统（注册、登录、认证、积分、用户管理）
- **测试环境**：PostgreSQL阿里云测试库
- **测试人员**：全流程测试子代理

## 测试执行统计

| 测试类型 | 用例总数 | 通过 | 失败 | 通过率 |
|---------|---------|------|------|--------|
| 单元测试 | XX | XX | 0 | 100% |
| 接口测试 | XX | XX | 0 | 100% |
| E2E自动化测试 | XX | XX | 0 | 100% |
| 手工验证 | XX | XX | 0 | 100% |
| **合计** | **XX** | **XX** | **0** | **100%** |

## 测试覆盖率

- 单元测试覆盖率：XX%
- 接口覆盖率：100%
- 核心页面覆盖率：100%

## 问题统计与修复情况

| 问题级别 | 发现数量 | 已修复 | 闭环率 |
|---------|---------|--------|--------|
| P0 阻断 | XX | XX | 100% |
| P1 严重 | XX | XX | 100% |
| P2 一般 | XX | XX | XX% |
| P3 建议 | XX | XX | XX% |

## 关键验证项结果

1. ✅ 所有接口对接真实后端，无Mock数据
2. ✅ 前后端字段100%一致
3. ✅ 权限拦截机制正常工作
4. ✅ Token过期处理正确
5. ✅ 积分并发操作安全（乐观锁生效）
6. ✅ 所有核心业务流程顺畅

## 遗留风险评估

### 已知未修复问题

| 问题ID | 级别 | 描述 | 影响评估 | 计划修复版本 |
|--------|------|------|----------|-------------|
| ISS-XX | P2 | ... | 低 | V1.1 |

### 潜在风险

1. ...

## 验收结论

✅ **验收通过** - 阶段一核心用户系统所有功能符合需求，质量达到上线标准

## 上线建议

1. 上线前执行一次全量数据库备份
2. 配置监控告警，关注登录和积分相关接口
3. 灰度发布，先开放10%用户注册
4. 上线后7x24小时监控运行状态

## 附件

1. 单元测试覆盖率报告
2. E2E自动化测试HTML报告
3. 问题追踪表
4. Mock接口清理报告
5. 前后端字段一致性核对表
```

- [ ] **Step 2: 整合所有测试结果和报告**

填充报告中的统计数据，附上所有附件

- [ ] **Step 3: 生成最终报告并Commit**

```bash
git add docs/test-reports/阶段一核心用户系统验收测试报告.md
git commit -m "test: 阶段一验收测试报告完成"
```

---

## 计划完成自检清单

- [ ] 所有任务步骤代码完整，无占位符
- [ ] 所有文件路径准确无误
- [ ] 所有测试命令正确，预期结果明确
- [ ] 任务间依赖关系清晰
- [ ] 测试分层覆盖完整（单元/接口/E2E/手工）
- [ ] 问题闭环流程明确
- [ ] 报告输出内容完整，符合验收级要求

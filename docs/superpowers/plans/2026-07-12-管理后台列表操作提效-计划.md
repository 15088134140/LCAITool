# 管理后台列表操作提效 Implementation Plan

> **For agentic workers:** 按 Task 顺序实施,checkbox 跟踪进度。每批次末尾统一 `git commit`(子代理不得单独 commit)。改动后运行 `codegraph sync`。

**Goal:** 修复管理后台 9 个列表页的真 bug,统一搜索/分页/操作反馈行为,补可访问性与批量操作,抽取最小共享件(`useDebouncedValue`、`<Pagination>`、`<Modal>`、`<EmptyState>`)。

**Spec:** `docs/superpowers/specs/2026-07-12-管理后台列表操作提效-设计.md`

**Tech Stack:** React 18 + Vite + TypeScript + Tailwind + Zustand + React Router + Lucide;共享包 `@lcaitool/ui`(shadcn/ui 风格:cva + Radix Slot + `cn`)

---

## 文件结构总览

```
packages/ui/src/
├── pagination.tsx              # 新增:统一分页器(跳页+pageSize)
├── modal.tsx                   # 新增(批次2):统一 Modal
└── index.ts                    # 修改:导出新组件

apps/frontend-admin/src/
├── hooks/
│   └── useDebouncedValue.ts    # 新增:防抖 hook
├── components/ui/
│   ├── EmptyState.tsx          # 新增(批次2):空状态
│   └── Toast.tsx               # 现有(复用)
├── pages/
│   ├── tools/index.tsx         # 修改:搜索防抖+toast+Pagination
│   ├── orders/List.tsx         # 修改:修 bug+搜索防抖+Pagination
│   ├── reviews/index.tsx       # 修改:搜索防抖+分页守卫+Pagination
│   ├── UserManagement.tsx      # 修改:搜索防抖+分页守卫+Pagination
│   ├── verifications/index.tsx # 修改:Pagination
│   ├── refunds/index.tsx       # 修改:Pagination
│   ├── ideas/index.tsx         # 修改:Pagination
│   └── feedback/index.tsx      # 修改:Pagination
```

---

# 批次1:修 bug + 搜索统一 + 分页增强

## Task 1: useDebouncedValue hook

**Files:**
- Create: `apps/frontend-admin/src/hooks/useDebouncedValue.ts`

- [ ] **Step 1: 实现 hook**

```ts
import { useEffect, useState } from 'react';

/** 防抖 hook:返回防抖后的值,内部用 useEffect 稳定定时器。 */
export function useDebouncedValue<T>(value: T, delay = 300): T {
  const [debounced, setDebounced] = useState(value);
  useEffect(() => {
    const timer = setTimeout(() => setDebounced(value), delay);
    return () => clearTimeout(timer);
  }, [value, delay]);
  return debounced;
}
```

- [ ] **Step 2: 在 hooks/index 导出(若存在 index)**

**验证:** `pnpm lint`(admin)

---

## Task 2: <Pagination> 组件

**Files:**
- Create: `packages/ui/src/pagination.tsx`
- Modify: `packages/ui/src/index.ts`(导出)

- [ ] **Step 1: 实现组件**

Props:`page`、`pageSize`、`total`、`pageSizeOptions?: number[]`(默认 [10,20,50])、`onPageChange`、`onPageSizeChange`、`showQuickJumper?`、`showSizeChanger?`。
行为:
- `total === 0` 时不渲染(由调用方或组件内部判断)
- 页码窗口(5 页)+ 首末页 + 上下页箭头
- 跳页输入框(回车/失焦生效,clamp 到 [1,totalPages])
- pageSize 选择器(改变时回到第 1 页)
- 使用 `cn` + 现有语义色(`bg-primary text-primary-foreground` active),不硬编码 hex
- 复用现有 `buttonVariants`/`Input` 风格保持一致

- [ ] **Step 2: 导出**

在 `packages/ui/src/index.ts` 加 `export * from "./pagination";`

**验证:** `pnpm lint`(`@lcaitool/ui` + admin)

---

## Task 3: orders/List.tsx 修复与迁移

**Files:** `apps/frontend-admin/src/pages/orders/List.tsx`

- [ ] **Step 1: 修统计卡片 bug**(:248,254,260)
  移除"待支付/已支付/已退款"三张分状态卡片(基于当前页,错误),保留"全部订单"=`total`。改为单张或加注释说明需后端接口。

- [ ] **Step 2: 修 debounce 失效**(:60-62)
  删除 `debouncedSearch = debounce(...)`,改用 `useDebouncedValue(searchInput, 300)`,useEffect 依赖 debounced 值触发 `setParams({keyword})`。

- [ ] **Step 3: 用 `<Pagination>` 替换手写分页**(:350-394)
  传入 `page=params.page`、`pageSize=params.pageSize`、`total`、`onPageChange`、`onPageSizeChange`。移除 `totalPages`/`totalPages||1` 兜底逻辑(组件内部处理)。

- [ ] **Step 4: 空数据不渲染分页栏**(组件内部 `total===0` 不渲染,确认移除旧守卫)

**验证:** 手动核验搜索防抖、跳页、pageSize 切换、空数据无分页栏

---

## Task 4: tools/index.tsx 迁移

**Files:** `apps/frontend-admin/src/pages/tools/index.tsx`

- [ ] **Step 1: 搜索改防抖**(:64-66)
  `handleSearch` 改为只 `setKeyword`;用 `useDebouncedValue(keyword, 300)` 得 debounced;useEffect 依赖 debounced 触发 `setParams({page:1, keyword: debounced})`。

- [ ] **Step 2: 补 toast**(:51-62 loadTools、:88-96 toggleStatus)
  catch 块加 `toast.error('加载工具列表失败')` / `toast.error('切换状态失败')`;toggle 成功加 `toast.success`。

- [ ] **Step 3: 用 `<Pagination>` 替换**(:354-400)

**验证:** 搜索防抖、toast、跳页

---

## Task 5: reviews/index.tsx 迁移

**Files:** `apps/frontend-admin/src/pages/reviews/index.tsx`

- [ ] **Step 1: 搜索改防抖**(:135-137)
- [ ] **Step 2: 分页守卫 + 用 `<Pagination>`**(:358,移除始终显示;组件替换)

**验证:** 搜索防抖、空数据无分页栏

---

## Task 6: UserManagement.tsx 迁移

**Files:** `apps/frontend-admin/src/pages/UserManagement.tsx`

- [ ] **Step 1: 搜索改防抖**(:101-107)
- [ ] **Step 2: 分页守卫 + 用 `<Pagination>`**(:429)

**验证:** 同上

---

## Task 7: verifications/index.tsx 迁移

**Files:** `apps/frontend-admin/src/pages/verifications/index.tsx`

- [ ] **Step 1: 用 `<Pagination>` 替换 Variant A 分页**(:238-284)

**验证:** 跳页、pageSize

---

## Task 8: refunds/ideas/feedback 迁移(Variant B)

**Files:** `refunds/index.tsx`、`ideas/index.tsx`、`feedback/index.tsx`

- [ ] **Step 1: 三页用 `<Pagination>` 替换 Variant B 分页**
  refunds:237-259;ideas:309-331;feedback:300-322

**验证:** 跳页、pageSize(原 Variant B 无页码,升级为带页码)

---

## Task 9: 批次1 验证与提交

- [ ] **Step 1: lint + build**
```bash
cd apps/frontend-admin && pnpm lint && pnpm build
```
- [ ] **Step 2: codegraph sync**
- [ ] **Step 3: 批次末尾统一 commit**
```bash
git add -A && git commit -m "feat(admin): 列表页搜索防抖/分页增强/修复统计与防抖bug"
```

---

# 批次2:操作安全 + 可访问性 + 加载体验

## Task 10: <Modal> 组件 + 迁移
- 新增 `packages/ui/src/modal.tsx`(Props:title/children/onClose/onConfirm/confirmText/confirmVariant,保留 tools 全 props)
- 迁移 7-8 页内联 Modal:tools、refunds、verifications、reviews、ideas、feedback、categories、UserManagement
- 各页特殊 props 保留(verifications/ideas 驳回原因输入)

## Task 11: 操作列可访问性 + 切换操作 toast
- 操作列图标按钮补 `aria-label`(或 `aria-label` + `title`)
- 删除等危险操作改「图标+文字」
- 切换操作(上下线/显示隐藏/启用禁用)改即时切换 + `toast.success`/`toast.error`;删除保留确认 Modal
- (可选)undo:若 Toast 易加 action 则实现,否则降级

## Task 12: EmptyState + skeleton + 表头 sticky
- 新增 `<EmptyState>`,替换 8 页"暂无数据"文字(加引导文案)
- 表格 loading 改 skeleton(替换"加载中..."文字)
- 表头 `sticky top-0`

## Task 13: 批次2 验证与提交
- `pnpm lint && pnpm build`、`codegraph sync`、commit

---

# 批次3:批量操作

## Task 14: tools 批量操作
- 表格加 checkbox 列 + 全选;选中态维护
- 批量工具栏(选中 >0 时显示):批量上线/下线/删除
- 前端循环调用 `toolApi.toggleStatus`/`toolApi.delete` + 进度 toast(已完成/总数)+ 结果汇总(成功/失败数)

## Task 15: 复制到 reviews/UserManagement
- reviews:批量显示/隐藏
- UserManagement:批量启用/禁用

## Task 16: 批次3 验证 + review 文档
- `pnpm lint && pnpm build`、`codegraph sync`、commit
- 写 `docs/superpowers/reviews/2026-07-12-管理后台列表操作提效-审查.md`
- 任务文档移至 `tasks/done/`

---

## Plan 自检

### Spec 覆盖
- ✅ 真 bug#1(orders 统计) -> Task 3 Step1
- ✅ 真 bug#2(orders debounce) -> Task 3 Step2
- ✅ 真 bug#3(空数据分页) -> Task 3/5/6
- ✅ 搜索统一防抖 -> Task 1 + Task 3/4/5/6
- ✅ `<Pagination>` 8 页 -> Task 2 + Task 3-8
- ✅ tools 补 toast -> Task 4 Step2
- ✅ `<Modal>` 迁移(批次2) -> Task 10
- ✅ 操作列 aria + 切换 toast(批次2) -> Task 11
- ✅ EmptyState/skeleton/sticky(批次2) -> Task 12
- ✅ 批量操作(批次3) -> Task 14/15

### 边界检查
- ✅ 不抽 DataTable/FilterBar/StatusBadge/Form
- ✅ 不改后端、API 契约
- ✅ 不改表单页
- ✅ ideas/feedback 回车搜索保留(不强制改防抖)
- ✅ categories 客户端过滤不动
- ✅ 裸 `debounce` 不改,用 hook 替代

# 创意视频校验前置 Implementation Plan

> **For agentic workers:** 按 Task 顺序实施，checkbox 跟踪进度。改动后运行 `codegraph sync`。子代理不得单独 commit。

**Goal:** 把创意视频参数校验从 worker 前置到 `create_task` API 层，校验失败返回 400，前端 toast 即时提示；复用现有 `_validate_params`，不改动前端。

**Spec:** `docs/superpowers/specs/2026-07-13-创意视频校验前置-设计.md`

**Tech Stack:** Python 3.11 + FastAPI + SQLAlchemy(async) + Celery

---

## 文件结构总览

```
apps/backend/app/api/v1/endpoints/
└── tasks.py                    # 修改：新增 _validate_task_params，create_task 调用

apps/backend/tests/
└── (新增) create_task 校验用例   # 文生视频空 prompt 400 等
```

---

## Task 1: 新增 _validate_task_params 辅助函数

**Files:** `apps/backend/app/api/v1/endpoints/tasks.py`

- [ ] **Step 1: 在 `_calculate_task_cost` 下方新增 `_validate_task_params`**

```python
async def _validate_task_params(db: AsyncSession, task_in: TaskCreate) -> None:
    """创建任务前提前校验参数，避免无效任务进入队列后才报错。

    复用各执行器的 _validate_params（若存在）。校验失败抛出 400，
    由前端 useToolGeneration 的 toast 直接提示用户。
    """
    from fastapi import HTTPException
    from app.executors.registry import get_executor_class

    if not task_in.tool_id:
        return

    result = await db.execute(select(Tool).where(Tool.id == task_in.tool_id))
    tool = result.scalar_one_or_none()
    if not tool:
        return

    executor_key = tool.executor_key or task_in.task_type
    executor_class = get_executor_class(executor_key)
    # 仅当执行器实现了 _validate_params 时校验（目前仅创意视频）
    if not executor_class or not hasattr(executor_class, "_validate_params"):
        return

    # 构造与 worker 一致的工具配置，供 _validate_params 读取（不局限于特定执行器）
    tool_config = {
        "base_fee": tool.base_fee,
        "image_fee": tool.image_fee,
        "audio_fee": tool.audio_fee,
        "token_fee": tool.token_fee,
        "is_mock_enabled": tool.is_mock_enabled,
        "is_prompt_logging_enabled": tool.is_prompt_logging_enabled,
    }
    # _validate_params 是纯参数校验，不依赖 task_id/db；用占位 task_id 实例化
    executor = executor_class(task_id=uuid.uuid4(), db=db, tool=tool_config)
    try:
        executor._validate_params(task_in.input_params or {})
    except ValueError as e:
        raise HTTPException(status_code=400, detail=str(e))
```

**验证:** import 检查（`Tool`、`uuid`、`select`、`AsyncSession` 均已在文件顶部导入，无需新增 import）

---

## Task 2: create_task 端点调用校验

**Files:** `apps/backend/app/api/v1/endpoints/tasks.py`

- [ ] **Step 1: 在 `create_task` 中插入校验调用**

定位 `_calculate_task_cost` 之后、`TaskService.create_task` 之前（tasks.py:97-103 附近）：

```python
    # 后端重新计算费用（不信任前端传入的 estimated_cost）
    estimated_cost = await _calculate_task_cost(db, task_in)

    # 覆盖前端传入的 estimated_cost
    task_in.estimated_cost = estimated_cost

    # 提前校验参数：在创建任务和冻结积分之前，复用执行器校验
    await _validate_task_params(db, task_in)

    # 创建任务（含预冻结积分）
    task = await TaskService.create_task(db=db, task_in=task_in)
```

**验证:** 启动服务，确认无导入/语法错误

---

## Task 3: 后端单测

**Files:** `apps/backend/tests/`（新增或扩展既有创意视频/任务 API 测试）

- [ ] **Step 1: 新增 create_task 校验用例**
  - 文生视频模式（无 first_frame/last_frame）+ 空 prompt -> 400，detail 含"创意描述必填"
  - 上传首帧 + 空 prompt -> 正常创建
  - 上传首尾帧 + 空 prompt -> 正常创建
  - 其他工具（如绘本）创建 -> 不受影响（无 `_validate_params`）

- [ ] **Step 2: 确认不创建任务/不冻结积分**
  - 400 场景下，断言无 Task 记录、无积分冻结记录

**验证:** `pytest tests/ -k "create_task or creative_video" -v`

---

## Task 4: 验证与收尾

- [ ] **Step 1: codegraph sync**
```bash
codegraph sync
```
- [ ] **Step 2: 手测**（启动后端 + 前端）
  - 创意视频页，不上传参考图、留空创意描述、点生成 -> toast 提示"文生视频模式下创意描述必填"，不进入进度弹窗
  - 上传首帧后留空创意描述、点生成 -> 正常进入进度弹窗
- [ ] **Step 3: 写 review 文档**
  - `docs/superpowers/reviews/2026-07-13-创意视频校验前置-审查.md`
- [ ] **Step 4: 任务文档移至 done**
  - `tasks/in-progress/2026-07-13-创意视频校验前置.md` -> `tasks/done/`

---

## Plan 自检

### Spec 覆盖
- ✅ API 层提前校验 -> Task 1 + Task 2
- ✅ 复用 _validate_params，不重复实现 -> Task 1（hasattr + 调用）
- ✅ 不改动前端 -> 无前端 Task
- ✅ 不改 creative_video.py -> 无该文件 Task
- ✅ 不为其他执行器补 _validate_params -> 无

### 边界检查
- ✅ 不扩展前端条件必填
- ✅ 不修改 worker autoretry（可选项，默认不纳入）
- ✅ 不改 API 契约（create_task 仍返回 TaskSchema，仅增加 400 错误路径）
- ✅ 其他执行器不受影响（hasattr 跳过）
